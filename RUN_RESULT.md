# RUN_RESULT

| 字段 | 值 |
| --- | --- |
| 是否已运行 | 否 |
| 运行时间 | — |
| 是否成功 | — |
| 模型 size | tiny（默认，未实跑） |
| 音频时长 | — |
| 识别语言 | — |
| 转写耗时 | — |
| 错误原因 | — |

## 备注

- 仓库已生成，**未实际运行**。
- 不实跑的原因：
  1. 仓库内不包含真实音频（`samples/*.wav` 已在 `.gitignore`）。
  2. 第一次运行会下载 ~75MB 的 tiny 权重，需要你确认网络可用。
  3. 还需要本机 `ffmpeg`，请确认已装。
- 跑通后请把表里的字段回填。

## 运行日志（你跑完后手动追加）

```
（在这里粘贴 main.py 的终端输出片段）
```
